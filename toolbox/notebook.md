# Just a collection of small tidbits

Change timezone on a Debian system:
```
# dpkg-reconfigure tzdata
```

Set rainbow using `msi-keyboard`
```
# msi-keyboard -m normal -c left,red,high middle,purple,high right,sky,high
```
